asgiref==3.11.0
Django==6.0.1
django-environ==0.12.0
django-jazzmin==3.0.1
django-session-timeout==0.1.0
pillow==12.1.0
razorpay==1.4.1
sqlparse==0.5.5
tzdata==2025.3

# ── Production dependencies ────────────────────────────────────────────
# These are needed at deploy time (Render/Linux). Locally they are optional:
# the app runs on SQLite + the local filesystem unless the matching env flags
# are enabled, so they do not need to be installed for local development.
gunicorn==23.0.0          # WSGI server (production) — Linux only
psycopg[binary]==3.2.3     # PostgreSQL driver (Neon) when DATABASE_URL is set
whitenoise==6.8.2          # production static-file serving (USE_WHITENOISE=True)
django-cloudinary-storage==0.3.0   # Cloudinary media (USE_CLOUDINARY=True)
cloudinary==1.41.0
